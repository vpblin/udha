# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.swiftpm/
Packages/
Package.resolved

# CocoaPods (unused but kept for safety)
Pods/

# Profiling artifacts
*.profraw
*.profdata

# Local Claude Code settings (machine-specific)
.claude/
CLAUDE.md

# Logs
*.log

# graphify knowledge graph output
graphify-out/

# Per-machine signing / bundle id (see Udha.xcconfig)
Local.xcconfig

# Stray renders at the repo root
/*.png

# The audit's private pattern list — the names being scrubbed must not ship
# with the scrubber (see scripts/release/audit.sh).
scripts/release/audit-private.txt
